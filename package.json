{
  "name": "trail-blazers",
  "version": "1.0.0",
  "description": "InterviewTrail é uma plataforma web de simulação de entrevistas técnicas personalizadas para candidatos em início de carreira. O que faz: A aplicação conecta ao GitHub do candidato, analisa seu portfólio de projetos e a descrição da vaga desejada, e gera automaticamente uma entrevista técnica simulada utilizando inteligência artificial ge...",
  "main": "index.js",
  "scripts": {
    "prepare": "husky",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build:backend": "npm --prefix backend run build",
    "build:frontend": "npm --prefix frontend run build",
    "build": "npm run build:backend && npm run build:frontend",
    "lint": "npm --prefix backend run lint && npm --prefix frontend run lint",
    "test": "npm --prefix backend run test && npm --prefix frontend run test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/juninhos-comunidade/trail-blazers.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/juninhos-comunidade/trail-blazers/issues"
  },
  "homepage": "https://github.com/juninhos-comunidade/trail-blazers#readme",
  "devDependencies": {
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0",
    "commitlint": "^21.2.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0"
  },
  "lint-staged": {
    "backend/**/*.{ts,js}": [
      "npm --prefix backend run lint -- --fix"
    ],
    "frontend/**/*.{ts,tsx,js,jsx}": [
      "npm --prefix frontend run lint -- --fix"
    ],
    "**/*.{json,md,yml,yaml}": [
      "npx prettier --write"
    ]
  },
  "dependencies": {
    "@nestjs/mapped-types": "*",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.15.1"
  }
}
